# default: on
# description: The calculator server
service balance
{
    flags       = REUSE
    socket_type = stream        
    wait        = no
    user        = nobody
    server      = /usr/local/bin/balance
    disable     = no
}